[pytest]
asyncio_mode = strict
timeout = 60
timeout_method = signal
markers =
    smoke: multi-replica smoke subset gated by ci-gate (required check)
    smoke_wps: multi-replica + WPS-transport smoke subset (gates ci-gate)
    thumbnail: composed-slide real-render thumbnail test; needs Playwright+Chromium (own ci-gate job)
